---
- block:
    - name: ping
      ping:
      register: pinged
      failed_when: "'UNREACHABLE' in pinged.stderr"
  rescue:
    - debug: var="FAILED"
  always:
    - name: Print if pinged
      debug: var=pinged
